TO DO LIST:
-Change loadMap method so that it loads files exported from Tiled.(currently loads map from simplified file)
-Change the load Resources part in level so that the tileset directory comes from the Tiled file aswell.
-Change main game class. Get a class that contains all of the game resources. 
-Get some basic audio
-Get Mobs and all that crap
-Implement max fps without affecting movement of stuff
-CHeck ways to imrpove drawing performance... check why the fuck when u use a pointer for 2nd tileset it increases speed by a lot.
-Make it so that no class actually loads the image, they simply use the one loaded in GameResource class
-Add a command interpreter that will read a file in order to add all objects
-Make it so that when loading